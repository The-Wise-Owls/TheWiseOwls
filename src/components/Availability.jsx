import React, { useState } from 'react';
import { withRouter } from 'react-router-dom';

const Availability = () => {

  return (
    <div style={{color: 'white', fontSize: '50px'}} id="templateTest">Availability</div>
  );

}

export default withRouter(Availability);